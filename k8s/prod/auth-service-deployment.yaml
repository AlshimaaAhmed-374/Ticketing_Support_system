apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.prod.yml
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: auth-service
  name: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: auth-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.prod.yml
        kompose.version: 1.38.0 (a8f5d1cbd)
      labels:
        io.kompose.service: auth-service
    spec:
      containers:
        - envFrom:
            - configMapRef:
                name: auth-service--env-prod
          image: docker-auth-service:latest
          imagePullPolicy: IfNotPresent
          name: auth-service
          ports:
            - containerPort: 7001
              protocol: TCP
      restartPolicy: Always
